const sum = (a, b) => {
  console.log(a);
  console.log(b);
  return a + b;
};

const result = sum(1, 5);
console.log(result);

//Si hay un parametro
const square = (p) => p * p;

const t = [1, 2, 3];
const tSquared = t.map((p) => p * p);

function product(a, b) {
  return a * b;
}

const avarage = function (a, b) {
  return (a + b) / 2;
};
